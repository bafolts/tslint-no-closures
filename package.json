{
  "name": "memory",
  "version": "1.0.0",
  "description": "",
  "main": "memory.js",
  "scripts": {
    "compile": "tsc",
    "lint": "tslint --project tsconfig.json --type-check bad.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tslint": "^5.2.0",
    "typescript": "^2.3.2"
  }
}
